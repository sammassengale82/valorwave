import React from "react";

const VenueMode: React.FC = () => {
  return (
    <div>
      <h2>Venue Mode</h2>
      <p>Multi-screen venue controls will appear here.</p>
    </div>
  );
};

export default VenueMode;
